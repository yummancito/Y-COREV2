# Steam — canales IPC

Generado desde `packages/ipc-contract` — ver
[`docs/01-architecture/ipc-contract.md`](../../01-architecture/ipc-contract.md), sección
`Namespace steam.*`. No se edita a mano aquí: para cambiar la doc de un canal, edita su
`.describe()` en `packages/ipc-contract/src/channels/steam.ts` y corre
`pnpm --filter @ycore/desktop docs:ipc`.

Canales de esta feature: `steam.importLibrary`.
