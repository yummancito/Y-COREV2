{
  "name": "@ycore/web-landing",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Landing 'próximamente' de Y-CORE, estática, en Astro.",
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro check && astro build",
    "typecheck": "astro check",
    "lint": "astro check",
    "test": "echo \"sin tests: sitio estático sin lógica\" && exit 0",
    "preview": "astro preview --host 0.0.0.0"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@ycore/tsconfig": "workspace:*"
  }
}
