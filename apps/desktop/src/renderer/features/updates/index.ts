/**
 * `renderer/features/updates` — API pública explícita.
 *
 * Sirve para que `App.tsx` monte el banner sin conocer la estructura interna
 * de la feature.
 */

export { UpdateBanner } from './components/UpdateBanner.js';
