/**
 * `renderer/features/settings` — API pública explícita.
 *
 * Sirve para que `App.tsx` monte el panel sin conocer la estructura interna
 * de la feature.
 */

export { SettingsPanel } from './components/SettingsPanel.js';
